import React from 'react';
import PageTemplate from '../PageTemplate';

/**
 * Locations page component
 */
const LocationsPage: React.FC = () => {
  return (
    <PageTemplate 
      title="Locations" 
      description="Manage church locations and campuses."
    />
  );
};

export default LocationsPage;
